import { TEMPLATE_DEFINITIONS } from '@vowly/types'

/**
 * Template registry.
 *
 * Currently only the `floral` template is active.
 * Classic and luxury are removed for now — can be re-added later.
 */
export const templateDefinitions = TEMPLATE_DEFINITIONS
