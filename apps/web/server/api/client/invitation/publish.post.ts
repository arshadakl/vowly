import { buildBaseSlug, resolveSlug } from '@vowly/utils'
import { editorContext } from '../../../utils/editor'
import { apiError } from '../../../utils/http'
import { getEnv } from '../../../utils/env'
export default defineEventHandler(async (event) => { const context = await editorContext(event); if (context.locked || context.client.status === 'READ_ONLY') apiError('EDIT_LOCKED', 'This invitation is locked.', 403); if (context.invitation.published && context.invitation.slug) return { slug: context.invitation.slug, published: true, publishedAt: context.invitation.published_at }; const db = getEnv(event).DB; const base = buildBaseSlug(context.invitation.bride_name, context.invitation.groom_name, context.client.wedding_date); const rows = await db.prepare('SELECT slug FROM invitations WHERE slug IS NOT NULL').all<{ slug: string }>(); const slug = context.invitation.slug ?? resolveSlug(base, rows.results.map((row) => row.slug)); const publishedAt = new Date().toISOString(); const ogImageUrl = `${getRequestURL(event).origin}/api/public/invitations/${slug}/og.svg?v=${encodeURIComponent(publishedAt)}`; await db.prepare('UPDATE invitations SET slug = ?, published = 1, published_at = ?, og_image_url = ?, updated_at = ? WHERE id = ? AND client_id = ?').bind(slug, publishedAt, ogImageUrl, publishedAt, context.invitation.id, context.client.id).run(); return { slug, published: true, publishedAt, ogImageUrl } })
