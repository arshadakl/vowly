import { clientUpdateSchema } from '@vowly/types'
import { apiError, body, requireAdmin } from '../../../utils/http'
import { getEnv } from '../../../utils/env'
export default defineEventHandler(async (event) => { await requireAdmin(event); const parsed = clientUpdateSchema.safeParse(await body(event)); if (!parsed.success || !Object.keys(parsed.data).length) apiError('INVALID_INPUT', 'Provide at least one valid client field.', 400); const fields = parsed.data; const updates: string[] = []; const values: string[] = []; if (fields.name !== undefined) { updates.push('name = ?'); values.push(fields.name) } if (fields.phone !== undefined) { updates.push('phone = ?'); values.push(fields.phone) } if (fields.weddingDate !== undefined) { updates.push('wedding_date = ?'); values.push(fields.weddingDate) } values.push(getRouterParam(event, 'id')!); const db = getEnv(event).DB; const result = await db.prepare(`UPDATE clients SET ${updates.join(', ')} WHERE id = ?`).bind(...values).run(); if (!result.meta.changes) apiError('NOT_FOUND', 'Client not found.', 404); const row = await db.prepare('SELECT * FROM clients WHERE id = ?').bind(getRouterParam(event, 'id')).first<Record<string, unknown>>(); return { id: row!.id, clientCode: row!.client_code, name: row!.name, phone: row!.phone, passcode: row!.passcode, status: row!.status, weddingDate: row!.wedding_date, weddingTz: row!.wedding_tz, createdAt: row!.created_at } })
