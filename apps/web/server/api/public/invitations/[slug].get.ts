import { apiError } from '../../../utils/http'
import { getEnv } from '../../../utils/env'
export default defineEventHandler(async (event) => { const slug = getRouterParam(event, 'slug'); const db = getEnv(event).DB; const row = await db.prepare("SELECT i.*, c.wedding_date, c.wedding_tz FROM invitations i JOIN clients c ON c.id = i.client_id WHERE i.slug = ? AND i.published = 1 AND c.status != 'DELETED'").bind(slug).first<{ id: string; bride_name: string; groom_name: string; slug: string; template: 'classic' | 'luxury'; quote: string | null; cover_image: string | null; bride_image: string | null; groom_image: string | null; wedding_date: string; wedding_tz: string; rsvp_enabled: boolean; og_image_url: string | null }>(); if (!row) apiError('NOT_FOUND', 'Invitation not found.', 404); const events = await db.prepare('SELECT * FROM events WHERE invitation_id = ? ORDER BY sort_order, id').bind(row!.id).all<{ id: string; invitation_id: string; title: string; event_date: string; start_time: string | null; end_time: string | null; venue: string | null; google_map: string | null; address: string | null; notes: string | null; sort_order: number }>(); return { id: row!.id, brideName: row!.bride_name, groomName: row!.groom_name, slug: row!.slug, template: row!.template, quote: row!.quote, coverImage: row!.cover_image, brideImage: row!.bride_image, groomImage: row!.groom_image, weddingDate: row!.wedding_date, weddingTz: row!.wedding_tz, events: events.results.map((item) => ({ id: item.id, invitationId: item.invitation_id, title: item.title, eventDate: item.event_date, startTime: item.start_time, endTime: item.end_time, venue: item.venue, googleMapUrl: item.google_map, address: item.address, notes: item.notes, sortOrder: item.sort_order })), rsvp: { enabled: Boolean(row!.rsvp_enabled) }, ogImageUrl: row!.og_image_url, studio: { name: 'Vowly', instagram: null, phone: null } } })
